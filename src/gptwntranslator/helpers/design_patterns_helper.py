"""Design patterns helper module."""


def singleton(cls):
    """Helper function to implement singleton pattern."""
    instances = {}

    def get_instance(*args, **kwargs):
        if cls not in instances:
            instances[cls] = cls(*args, **kwargs)
        return instances[cls]

    return get_instance